---
name: basic-source-checks

'on':
  pull_request:

jobs:
  basic-source-checks:
    name: Basic Source checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Resolve code_checking ref
        run: |
          REF="origin/main"
          if [ -f code-checking-ref ]; then
            REF="$(
              grep -v '^[[:space:]]*#' code-checking-ref \
                | sed '/^[[:space:]]*$/d' \
                | head -n 1
            )"
          fi
          if [ -z "${REF}" ]; then
            REF="origin/main"
          fi
          case "${REF}" in
            refs/*)
              FETCH_REF="${REF}"
              ;;
            origin/*)
              FETCH_REF="refs/heads/${REF#origin/}"
              ;;
            pull/*/head|pull/*/merge)
              FETCH_REF="refs/${REF}"
              ;;
            *)
              FETCH_REF="refs/heads/${REF}"
              ;;
          esac
          git -C ./__CODE_CHECKING_PATH__ fetch origin "${FETCH_REF}"
          git -C ./__CODE_CHECKING_PATH__ checkout FETCH_HEAD
          echo "[workflow] using code_checking ref: ${REF}"

      - name: Install linter tools
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
        run: |
          ./__CODE_CHECKING_PATH__/checks/install-linter-tools.sh \
            --library-root ./__CODE_CHECKING_PATH__ \
            --target-root . \
            --mode changed \
            --base-ref "${GITHUB_BASE_REF:-}"

      - name: Block tracked code-checking-ref
        id: guard_code_checking_ref
        continue-on-error: true
        run: |
          ./__CODE_CHECKING_PATH__/checks/guard-code-checking-ref.sh \
            --target-root .

      - name: Block active Jenkins @Library reference
        id: guard_jenkins_library_pin
        continue-on-error: true
        run: |
          ./__CODE_CHECKING_PATH__/checks/guard-jenkins-library-pin.sh \
            --target-root .

      - name: Run linters on changed files
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
        run: ./__CODE_CHECKING_PATH__/bin/run-linters.sh

      - name: Fail if any guard check failed
        if: >-
          ${{ always() &&
              (steps.guard_code_checking_ref.outcome == 'failure' ||
               steps.guard_jenkins_library_pin.outcome == 'failure') }}
        run: |
          echo "[workflow] one or more guard checks failed" >&2
          if [ "${{ steps.guard_code_checking_ref.outcome }}" = \
               "failure" ]; then
            echo "[workflow] code-checking-ref was tracked in this change" >&2
          fi
          if [ "${{ steps.guard_jenkins_library_pin.outcome }}" = \
               "failure" ]; then
            echo "[workflow] active Jenkins @Library reference found" >&2
          fi
          echo "[workflow] keeping the final job status failed after" >&2
          echo "[workflow] running the remaining checks" >&2
          exit 1
