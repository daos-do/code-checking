#!/usr/bin/env bash
set -euo pipefail

echo "code_checking setup-dev (shell)"
echo "Current minimal setup installs no packages by default."
echo "Use bin/run-checks.sh to run repository bootstrap checks."
